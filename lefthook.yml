# Refer for explanation to following link:
# https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md

assert_lefthook_installed: true
skip_lfs: true

output:
  - success # Print successful steps
  - failure # Print failed steps printing
  - execution # Print any execution logs
  - execution_out # Print execution output
  - meta # Print lefthook version
  - summary # Print summary block (successful and failed steps)
  # - empty_summary  # Print summary heading when there are no steps to run
  # - execution_info # Print `EXECUTE > ...` logging
  - skips # Print "skip" (i.e. no files matched)

pre-commit:
  parallel: false
  commands:
    format:
      glob: '*.{cjs,mjs,js,ts,jsx,tsx,json,jsonc}'
      run: pnpm --silent exec oxfmt --write {all_files}
    check:
      glob: '*.{cjs,mjs,js,ts,jsx,tsx,json,jsonc}'
      run: pnpm --silent exec oxfmt --check {staged_files}
    lint:
      files: git diff --name-only main
      glob: '*.{cjs,mjs,js,ts,jsx,tsx,json,jsonc}'
      run: pnpm --silent exec oxlint --fix {staged_files}
    typecheck:
      files: git diff --name-only main
      glob: '*.{cjs,mjs,js,ts,jsx,tsx,json,jsonc}'
      run: pnpm --silent exec tsc -b --noEmit
    # test-unit:
    #   glob: '*.{cjs,mjs,js,ts,jsx,tsx,json,jsonc}'
    #   run: pnpm --silent test
    # test-coverage:
    #   glob: '*.{cjs,mjs,js,ts,jsx,tsx,json,jsonc}'
    #   run: pnpm --silent test:coverage
    # test-e2e:
    #   glob: "*.{cjs,mjs,js,ts,jsx,tsx,json,jsonc}"
    #   exclude: '(^|/)(test|stories)\.(ts|tsx)$'
    #   run: pnpm --silent e2e-test:chrome
    # check-links:
    #   glob: '*.md'
    #   run: lychee --max-concurrency 3 {all_files}
    #   exclude: [CHANGELOG.md, CONTRIBUTING.md]

# pre-push:
#   commands:
#     npm-audit:
#       tags: frontend security
#       run: pnpm --silent audit --prod

pre-deploy:
  parallel: false
  commands:
    docker-build:
      run: |
        pnpm --silent docker:build
        pnpm --silent docker:images
